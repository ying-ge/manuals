# 🚨 GLM API 配置问题诊断和解决指南

## 📊 问题分析结果

**根本原因确认**：LLM API密钥缺失导致所有文章使用启发式提取

### 对比数据：
- **第一次运行**：0% 需要人工审查 (高质量LLM提取)
- **最近运行**：100% 需要人工审查 (低质量启发式提取)

## 🔧 解决方案

### 方案1：配置环境变量 (本地运行)

1. **创建 `.env` 文件**：
```bash
cp .env.example .env
```

2. **编辑 `.env` 文件**：
```bash
# 必须设置 GLM API 密钥
GLM_API_KEY=your_actual_glm_api_key_here
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
GLM_MODEL_NAME=glm-4.6

# 可选：OpenAI 备用
# OPENAI_API_KEY=your_openai_key_here
```

3. **获取GLM API密钥**：
   - 访问：https://open.bigmodel.cn/
   - 注册/登录账号
   - 进入 "API密钥" 页面
   - 创建新密钥

### 方案2：GitHub Actions 配置

1. **进入仓库设置**：
   - Settings → Secrets and variables → Actions

2. **添加Repository Secrets**：
   ```
   GLM_API_KEY: your_actual_glm_api_key_here
   GLM_BASE_URL: https://open.bigmodel.cn/api/paas/v4
   GLM_MODEL_NAME: glm-4.6
   ```

## 🧪 验证步骤

### 1. 清除缓存
```bash
python clear_cache.py
```

### 2. 测试配置
```bash
python test_glm_api.py
```

### 3. 检查日志
查看 `logs/harvest.log` 文件，应该看到：
- ✅ "GLM client initialized successfully"
- ✅ "Successfully extracted using GLM"
- ❌ 不应该看到 "Using heuristic extraction"

## 🎯 预期结果

配置正确后，应该看到：
- **中文详细描述**：300-500字
- **AI角色说明**：200-300字
- **低人工审查率**：< 5%
- **高质量提取**：完整的研究内容和AI应用分析

## 🚨 常见问题

### Q1: 为什么之前工作正常？
A1: 可能之前在API密钥正确配置的环境中运行，或者使用了不同的配置文件。

### Q2: GLM-4.6 和 glm-4-flash 有什么区别？
A2:
- `glm-4-flash`: 基础版本模型
- `glm-4.6`: 最新的高性能模型

### Q3: 如何确认API密钥有效？
A3: 运行测试脚本 `python test_glm_api.py`，会显示详细的连接状态。

## 📋 检查清单

- [ ] GLM_API_KEY 已设置
- [ ] GLM_BASE_URL 已设置
- [ ] GLM_MODEL_NAME 设置为 'glm-4.6'
- [ ] 清除了缓存文件
- [ ] 运行了测试脚本并看到成功消息
- [ ] 检查日志文件确认使用GLM而非启发式

## 💡 提示

1. **永远不要**将 `.env` 文件提交到Git
2. **定期检查** GitHub Actions 的secrets配置
3. **监控日志**以确认API调用状态
4. **备份**重要的API密钥

配置正确后，系统将恢复到第一次运行时的高质量状态！